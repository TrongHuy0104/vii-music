import React from 'react'
import { StyleSheet, Text, TouchableOpacity } from 'react-native'

const Homeeeee = ({ playlistItem, onPress }) => {
	return (
		<TouchableOpacity>
			<Text>abcccc</Text>
		</TouchableOpacity>
	)
}

const styles = StyleSheet.create({
	bannerContainer: {
		flex: 1,

		margin: 8,
	},
	bannerImage: {
		width: '100%',
		height: 180,
		borderRadius: 8,
		objectFit: 'cover',
	},
})

export default Homeeeee
